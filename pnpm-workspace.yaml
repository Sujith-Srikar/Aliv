# Flat node_modules (no symlinks) so @astrojs/vercel / @vercel/nft can copy
# dependencies on Windows without SeCreateSymbolicLinkPrivilege (Dev Mode/admin).
nodeLinker: hoisted

allowBuilds:
  esbuild: true
  sharp: true